services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=cat-game'
      - 'POSTGRES_PASSWORD=postgrespw'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
